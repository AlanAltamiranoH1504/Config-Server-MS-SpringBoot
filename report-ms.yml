report:
  template: "{company} was founded in {foundation_date} by {founder}. The websites from this company are {web_sites}"
feign: # CIRCUITE BREAKER PARA CONEXION A MS CAIDOS O SATURADOS
  circuitbreaker:
    enabled: true
fallback: 
  uri: "http://localhost:8787/companies-crud-fallback/api/v1/company/{idCompany}"
